# 数据包ID
packet_1:
  hook: normal
  entityType: ARMOR_STAND
  # all or self
  viewer: all
  # 数据包属性
  mate:
    - equip HEAD item_1
    - displayName " " false
    - visible false
  # 数据包动作
  action:
    - type collection 5
    - breakCount 10
    - addBreakCount only 3 itemCheck inv mainHand *'minecraft:wooden_hone'

packet_2:
  # 继承方法
  hook: this packet_1