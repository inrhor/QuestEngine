# 数据包ID
#   重复接受会被覆盖，因此发送多个数据包需要构造多个方法
packet_1:
  hook: normal
  entityType: ARMOR_STAND
  # all or self
  viewer: all
  mate:
    - equip head item_1
    - displayName " " false
    - visible false

packet_2:
  # 继承方法
  hook: this packet_1